apiVersion: v1
kind: Service
metadata:
  name: solar-system-service
  namespace: default
spec:
  type: NodePort
  selector:
    app: solar-system
  ports:
  - port: 3000
    targetPort: 3000
    nodePort: 30000
